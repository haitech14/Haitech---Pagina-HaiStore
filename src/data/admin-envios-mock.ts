import type { ShipmentRecord } from '@/types/shipping';

/** Sin datos demo; el panel de envíos arranca vacío hasta crear registros reales. */
export const ADMIN_ENVIOS_MOCK_SHIPMENTS: ShipmentRecord[] = [];

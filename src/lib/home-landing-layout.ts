/** Fondo uniforme de la landing (mismo tono que la sección Multifuncionales). */
export const HOME_LANDING_SURFACE_CLASS = 'bg-muted/30';

/**
 * Altura fija del hero home (slide landing + promos del carrusel).
 * Ambos slides deben usar la misma clase para que Embla no deje hueco vacío.
 */
export const HOME_LANDING_HERO_HEIGHT_CLASS =
  'h-[min(88vw,26rem)] sm:h-[min(52vw,28rem)] lg:h-[30rem] xl:h-[32rem]';
